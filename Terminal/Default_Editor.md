# Change Default Editor
    $ vim ~/.bashrc
        export EDITOR="nvim"
    $ source ~/.bashrc

    // Print Default Editor
        $ echo $EDITOR
    // Test if it work
        $ sudoedit /etc/host.conf
